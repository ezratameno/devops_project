pipeline {
    agent any 
    stages {
        stage('Checkout') { 
            steps {
                git 'https://github.com/ezratameno/devops_project'
            }
        }
        stage('Build') { 
            steps {
                sh('./build.sh')
            }
        }
        stage('Deploy') { 
            steps {
                sh('./deploy.sh')
            }
        }
    }
}